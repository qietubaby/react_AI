import { reducer as photos } from '../components/photoGallery/store'
import { reducer as board } from '../components/drawingBoard/store'
import { reducer as shape } from '../components/toolBox/store'

export default {
 photos,
 board,
 shape
}